<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wzy.arknights.dao.SixMapper">
    <resultMap id="BaseResultMap" type="com.wzy.arknights.model.AccountInfo" >
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="password" property="password" jdbcType="VARCHAR" />
    </resultMap>

<!--  根据qq查询抽卡信息  -->
    <select id="selectSixByQQ" resultMap="BaseResultMap">
        select qq,found_count,today_count
        from six WHERE qq = #{qq}
    </select>

    <!--  插入新qq，若已存在，则更新垫刀数和今日抽卡次数  -->
    <update id="updateSixByQQ">
        INSERT INTO six (qq, found_count, today_count)
        VALUES
            (#{qq,jdbcType=BIGINT}, #{six,jdbcType=INTEGER}, 1)
        ON DUPLICATE KEY UPDATE
        found_count = #{six,jdbcType=INTEGER},
        today_count = today_count + 1
    </update>
</mapper>
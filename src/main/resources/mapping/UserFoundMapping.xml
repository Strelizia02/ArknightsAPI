<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wzy.arknights.dao.UserFoundMapper">
    <resultMap id="BaseResultMap" type="com.wzy.arknights.model.UserFoundInfo" >
        <result column="qq" property="qq" jdbcType="BIGINT" />
        <result column="found_count" property="foundCount" jdbcType="INTEGER" />
        <result column="today_count" property="todayCount" jdbcType="INTEGER" />
    </resultMap>

<!--  根据qq查询抽卡信息  -->
    <select id="selectUserFoundByQQ" resultMap="BaseResultMap">
        select qq,found_count,today_count
        from user_found WHERE qq = #{qq}
    </select>

    <!--  插入新qq，若已存在，则更新垫刀数和今日抽卡次数  -->
    <update id="updateUserFoundByQQ">
        INSERT INTO user_found (qq, found_count, today_count)
        VALUES
            (#{qq,jdbcType=VARCHAR}, #{six,jdbcType=INTEGER}, 1)
        ON DUPLICATE KEY UPDATE
        found_count = #{six,jdbcType=INTEGER},
        today_count = today_count + 1
    </update>

    <update id="cleanTodayCount">
        UPDATE user_found SET today_count = 0
    </update>
</mapper>